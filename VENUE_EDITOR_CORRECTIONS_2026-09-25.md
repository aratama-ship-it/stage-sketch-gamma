# 劇場設定・平面図の修正メモ（2026-09-25）

1. 平面図の余白ドラッグで表示位置を動かす。図形上は Shift＋ドラッグまたは中ボタンドラッグで移動する。拡大・再描画後も移動先を保つ。劇場データと編集履歴は変えない。
2. プリセットを開いたときの標準表示は舞台と舞台袖を基準に最大限大きく合わせる。客席と会場外枠は初期表示範囲の計算から外し、縮小・移動すれば引き続き見えるようにする。
3. 劇場形式プリセット・設定列の初期幅を、その画面で許される最大値にする。ドラッグ・キーで変えた幅は従来どおり記憶し、リセットは新しい初期幅へ戻す。
4. 平面図の「5番または6番を選択してください」など、操作していないときの固定案内を消す。操作結果・エラーは短い通知として残す。
5. 劇場の立体プレビューの移動・縮小・拡大ボタンを画像の右下へ移し、平面図の操作ボタンと位置・寸法・余白を合わせる。
6. 2番「メインの形」で舞台床の色を茶・黒・グレーから選び、立体プレビューへ即時反映する。舞台本体と追加ステージは同色。既存劇場は茶色のまま、黒・グレーだけ任意項目として保存する。
7. 3Dタブの会場を劇場設定の選択中の規模・作成会場から組み立てる。客席区画、客席床の高さ・傾斜、袖の形と袖幕、舞台床色、外枠、壁、天井高を反映する。袖幕は立体プレビューと同じ導出部品から各位置を一度だけ生成し、旧3Dの固定袖幕・固定額縁は会場モデルがある場合に描かない。座席の点は全区画合計でおおむね1400を目安に間引き、床の多角形は元の形を保つ。劇場設定から3Dへ直接移った際に設定パネルが前面に残る表示順も修正する。

UIトークン: 左列 220〜480px、右側の確保幅 420px、列間 18px。画像内操作は右・下 10px、ボタン 44×44px、間隔と内側余白 4px。色は既存の `--desk`・`--desk-2`・`--line-dark`・`--brass`・`--milk` を再利用する。
舞台床色トークン: 茶 `#806247`（従来値）、黒 `#303030`、グレー `#777777`。選択肢は既存の3列ボタングループを使い、色見本は14px角、操作面は44px以上。色名と選択状態を併記する。見本境界の明色 `#bdb3a4` は黒に6.38:1、暗色 `#201b16` は茶に3.05:1・グレーに3.81:1。ラベルの `#f2ead6` とパネル `#201b16` は14.24:1（design-webのcontrast.mjs実測）。

状態: 1〜6を試験場ショー J-2 で画面確認。3は列幅480pxの初期表示とキー変更・リセット、4は固定案内の非表示、5は右下への配置と移動モード・拡大操作、6は茶・黒・グレーの選択、立体への反映、Undo/Redo、ショーへの反映と再読み込み後の黒の保持を確認。7は同ショーJ-2の作成会場で劇場設定の平面図・立体と3Dを比較し、袖幕の導出位置、グレーの舞台、錆色の客席床、舞台奥からの座席、劇場設定→3Dの切り替えを確認。会場モデルの規模選択、傾斜床、袖削除時の幕ゼロを含むテストと合わせて184件通過。

## v0.2.15 GitHub Pages 公開前の互換性確認

比較元は `origin/main` の `3a2697a`（v0.2.14）。床色は劇場ライブラリの任意項目 `floor.previewColor` として追加し、値のない旧会場は従来の茶色のまま描く。旧会場の読込、黒・グレーの保存と再読込は `tests/stage-venue-floor-color.test.mjs` で確認した。3D画面へ選択中の会場規模 `sizeId` を渡すが、ショーの保存処理、音源、共有・認証処理は今回変更していない。平面図のドラッグ移動は表示位置だけを変え、劇場データと編集履歴には加えない。

公開候補で `node --test --test-reporter=tap tests/*.test.mjs` は184件成功・失敗0・スキップ0。変更した JavaScript と生成スクリプトの `node --check`、`git diff --check` は成功。同梱「テスト: 全機能の試験場」J-2 のローカル画面で、客席床高欄が選択前から表示され、未選択では編集不可、客席を選ぶと値が現れて編集可能になることも確認した。変更した JS/CSS の参照版を更新し、Service Worker のキャッシュ世代は v371。Cloudflare Worker β版は今回の対象外。Safari実機、端末のオフライン復帰、大規模会場での長時間操作は未検証であり、ローカル検証だけでは保証しない。
