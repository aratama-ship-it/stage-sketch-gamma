# Worker assets come from the repository root. Never upload local credentials or Wrangler state.
/.wrangler/**
/tools/**
/.dev.vars*
/.env*
/node_modules/**

# Developer checks and their results are not Worker assets.
/tests/**
/.github/**
/regression-results/**
