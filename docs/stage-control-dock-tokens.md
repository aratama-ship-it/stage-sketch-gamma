# 舞台タブの常設操作欄 — 変更トークン

2026-09-22、既存画面の小修正。新しい配色・書体は導入せず、既存の舞台スケッチの値を継承する。

| 対象 | 従来 | 今回 |
|---|---:|---:|
| 操作欄の配置 | 中央の図の上 | 左列の「ショー」より上。右一列では右列先頭、1119px以下では図の上 |
| `--stage-control-dock-gap` | 横並びの群間 14px | 縦積みの群間・次パネルまで 10px |
| `--stage-control-dock-pad-block` | 0px | 上下 9px、群の区切り上 9px |
| `--stage-control-dock-pad-inline` | 0px | 左右 10px |
| `--stage-control-group-gap` | 8px | 6px |
| 枠・背景 | なし・透明 | 既存 `--line-dark` 1px・`--desk` |

「ツール」「表示するもの」の小見出しは既存 `--brass`（#9c823f）と 9px を維持する。`--desk`（#191512）上のコントラストは 4.91:1、青の黒（#141619）上は 4.90:1（`contrast.mjs`、2026-09-22）。操作ボタンの寸法・キーボード操作・保存値は変更しない。

確認: デスクトップ 1280px と 1080px の表示、右一列表示、同梱試験場 G-2 の矢印ツールと図・装置名の切替。design-lint は初回案内の劇場設定画面を測定して既存の可読性・タップ寸法の NG を報告したため、この操作欄の判定には使用していない。iPad PWA と物理端末は未確認。
